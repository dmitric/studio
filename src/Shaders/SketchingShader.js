import Shader from './Shader.js'

/**
* Sketching shader
*/
export default class SketchingShader extends Shader {
  
  constructor (name) {
    super(name || "Sketching")
  }

  prepare (ctx, data, palette) {

  }

  render (ctx, pixel, data, palette) {
    // TBA
  }
}